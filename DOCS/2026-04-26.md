
# 2026-04-26 점검 결과 + 권한 매트릭스 결정
> 클로드코드 작업: CC-instruction-2026-04-26-점검정리.md 참조

## 점검: 회원가입·로그인 플로우

### 발견 이슈
- 

### OK 확인된 것
- 

### 메모
### OK 확인된 것
- 회원가입 페이지 정상 표시 (역할 선택 5개, 입력칸, 약관 동의)
- 콘솔 추가 에러 없음
- 가입 시 역할 5개로는 부족 (학원 경리·실장·조교 등 빠짐)
  → decisions.md의 "직원 형태 분류 19개"와 연계 필요
  → 베타 후 보완 (지금은 진행)
### 테스트 계정
- 이메일: wldbsl3333@gmail.com
- 비밀번호: soul0411
- 역할: 선생님
- 가입일: 2026-04-26
 ### 발견 이슈
- ⚠️ profiles 테이블 행만 삭제 시 → auth.users 잔존 → "승인 대기" 화면에 영구히 갇힘
  → 정상 흐름인지 버그인지 검토 필요
  → 운영자가 수동 삭제 시에도 같은 문제 가능
  → 계정 탈퇴 기능에서는 둘 다 처리되는지 확인 필요
 ### 🔴 발견 이슈 (Critical)
- 승인 대기 탭에 대기 회원 안 보임 ("선생님 인증 시스템은 준비 중" 표시)
  → 선생님 탭에는 "대기중" 상태로 보이는데 승인 대기 탭은 비활성
  → 운영자가 신규 가입자 승인 못 함
  → 베타 전 필수 수정

### 🟡 발견 이슈 (Minor UX)
- 소속 기관 검색·선택 시 칸 두 개로 보임
  → 선택 후 검색창 숨김/비활성화 필요

### OK 확인된 것
- 학원 자동완성 검색 작동
- 가입 후 운영자 화면 선생님 목록에 정상 등록
- 운영자 회원 현황 페이지 디자인·기능 정상

- 신규 가입 직후 대시보드에 "2025년 1학기 · 3학년 2반" 자동 표시 → 어디서 온 데이터?

### OK 확인된 것
- 운영자 승인 후 로그인 정상 (대시보드 진입)
- 사이드바·상단바 구조 깔끔
- 회계 메뉴 자물쇠 표시 (캠퍼스 차단 정책 반영) ✅
- 콘솔 에러 없음

### 🟡 발견 이슈 (Minor)
- 신규 가입 보너스 1,000pt → 캠퍼스 5,000pt 정책(4/26) 미반영
- 가입 직후 대시보드 "2025년 1학기 · 3학년 2반" 자동 표시 → 데이터 출처 확인 필요
- AI 유사문제 생성 메뉴 자물쇠 없음 (정책 미적용 또는 의도?).

- 계정 탈퇴 시 auth.users 잔존 (profiles만 status: deleted 처리)
  → 30일 복구 정책 때문일 수 있음 (의도된 설계?)
  → decisions.md 약관: "탈퇴 후 30일간 복구 목적 보관 후 영구 삭제"
  → 30일 후 auth.users도 자동 삭제되는지 확인 필요 (cron job? 수동?)

### 🔴 발견 이슈 (Critical) - 외래키 무결성
- Supabase Authentication에서 사용자 일괄 삭제 시도 → DB error
- 원인: profiles 외래키에 ON DELETE CASCADE 없음
- 영향:
  - 운영자가 사용자 강제 삭제 못함
  - 30일 후 자동 영구삭제 트리거(있다면) 실패 가능성
  - 위장 deleted 상태가 계속 누적될 수 있음
- 대응 방향:
  1. profiles 및 관련 테이블에 ON DELETE CASCADE 추가
  2. 또는 삭제 전 관련 행 정리하는 함수 작성
  3. 베타 전 필수 처리
- 임시 대응: SQL Editor로 수동 정리 (테스트 데이터 제거용)

### 🔴🔴 발견 이슈 (Critical) - 시스템 차원
- profiles/users를 참조하는 외래키 37개 (ON DELETE CASCADE 없음)
- 영향:
  1. 사용자 강제 삭제 불가 (Supabase UI/API 모두 실패)
  2. 30일 후 자동 영구삭제 트리거 작동 시 실패 위험
  3. 개인정보 강제 파기 요청(GDPR/개인정보보호법) 처리 불가
  
- 의도된 설계? 분석:
  - account_deletions, recovery_requests 테이블 존재 → soft delete 의도 맞음
  - "탈퇴 후 30일 복구 가능" 약관과 일치
  - 그러나 영구삭제 시 데이터 정리 함수가 안 보임
  
- 베타 전 처리 방향:
  1. 37개 테이블에 ON DELETE CASCADE 적용 (단순)
  2. 또는 계정 영구삭제 함수 만들기 (안전, 권장)
     - delete_user_permanently(user_id) RPC 함수
     - 37개 테이블 순차 정리 → 마지막에 auth.users 삭제
  3. 30일 cron 트리거가 이 함수 호출하도록

### 🔴 발견 이슈 - 메일 시스템 (Critical, 한꺼번에)

1. 메일 본문에 소속 기관 변수 누락 ("소속 기관: ―")
   → DB엔 저장됨, 메일 템플릿 변수 매핑 버그

2. 메일 발신자가 개인 Gmail (yoonbro1927@gmail.com)
   → 베타 시 누구 메일인지 불명, 스팸함 직행 위험
   → 정식 도메인 메일 필요

3. EmailJS 기본 템플릿 영문 placeholder가 본문에 노출
   → "A message by has been received. Kindly respond..." 한국 사용자에게 이상함
   → EmailJS 템플릿 수정 필요

4. 메일 발송 = EmailJS 외부 서비스 (Supabase 내장 X)
   → 무료 플랜 한계 확인 필요 (월 200통?)
   → 본문 하단 "Email sent via EmailJS.com" 광고 문구 노출
   → 정식 런칭 시 Supabase SMTP 또는 SendGrid/Resend 전환 검토

5. 이메일 인증 링크 없음 - 자동 인증 미구현
   → 가짜 이메일로도 가입 가능
   → 검증 = 운영자 수동 승인 1단계뿐
   → 의도된 설계인지 확인 필요 (봉쌤 본인)

6. 캠퍼스 가입 보너스 5,000pt 정책 미반영 (현재 1,000P)
   → 4/26 결정사항 코드 미반영

### 메일 시스템 이슈 정리 (확정)

베타 전 처리 (필수):
- 메일 본문 변수 매핑 버그 (소속 기관 ―)
- EmailJS 템플릿 영문 placeholder 제거
- 발신자를 봉쌤 명의 메일로 (yoonbro1927@gmail.com 누구?)

베타 후 처리 (정식 런칭 전):
- 이메일 자동 인증 추가 (Supabase Auth 활용)
- EmailJS → Supabase SMTP/SendGrid/Resend 전환
- 도메인 메일 (noreply@mathiq...)

의도된 설계 (변경 없음):
- 운영자 수동 승인 모델 유지
- 베타 단계 검증 게이트 = 봉쌤 본인

### 🔴 발견 이슈 - 가입 티어 배정 로직 부재
- 모든 신규 가입자 = 캠퍼스(무료) 자동 배정
- decisions.md 4/26 정책 미반영:
  - 캠퍼스 = 대학생 과외 전용
  - 선생님/원장/공부방/교습소/학원 = 베이직+
- 가입 폼에 "업태 선택" 단계 부재
- 선생님으로 가입 → 캠퍼스로 떨어짐 → 학생 3명 제한 등 이상한 경험

### 가입 폼 보완 사항 통합 (베타 후)
- 업태 5개 선택 (대학생/개인과외/공부방/교습소/학원)
- 직원 형태 19개 분류 (decisions.md 4/26)
- 업태에 따라 티어 자동 배정
- 캠퍼스는 대학생 과외만 가능

### ✅ 캠퍼스 권한 검증 결과 (점검 진행 중)
- [x] 회계 차단 → 자물쇠 모달 정상 작동
  - 메시지: "베이직 전용 기능" 명확
  - "현재 티어: 캠퍼스 (무료)" 표시
  - [업그레이드 하기] CTA 자연스러움
- [x] 캘린더 사용 가능 (decisions.md 정책 반영 — 의도된 설계)
- [ ] 학생 등록 3명 제한 - 미검증
- [ ] AI 학생 진단 차단 - 미검증
- [ ] AI 유사문제 생성 - 미검증
- [ ] 학부모 문자 발송 (학생당 주 2회 제한) - 미검증
- [ ] 자료실 포인트 시스템 - 미검증
- [ ] 과외 매칭 연락처 조회 2,000pt - 미검증


### 🔴🔴🔴 발견 이슈 (Critical) - 모달 닫기 불가
- 베이직 전용 기능 모달 (회계 자물쇠 클릭 시 표시)
  - X 버튼 ❌
  - 닫기 버튼 ❌
  - 업그레이드 하기 버튼 ❌
- 콘솔 에러 없음 (조용히 깨짐)
- 영향: 사용자가 모달에 갇힘 → 새로고침/브라우저 종료가 유일한 탈출구
- 베타 죽이는 버그
- 베타 전 무조건 수정

추가 점검: ESC 키로 닫히는지 확인
- 닫힘 → onClick 이벤트만 누락 (수정 쉬움)
- 안 닫힘 → 모달 상태 관리 자체 깨짐

🔴 베이직 전용 기능 모달 닫기 불가 (Critical)
- X / 닫기 / 업그레이드 / ESC 모두 작동 안 함
- 콘솔 에러 없음
- 페이지 새로고침으로만 탈출 가능
- 모달 상태 관리 자체 깨진 듯
- 동일 모달이 다른 자물쇠 메뉴에서도 같은 버그 있을 가능성 → 클로드코드가 일괄 점검 필요


## 점검 결과 — 사이드바 전 메뉴 (2026-04-26 야간)

### 🔴 Critical
1. 자물쇠 모달 닫기 불가 (회계·학생관리 동일 패턴 — 단일 컴포넌트 추정)
   - X / 닫기 / 업그레이드 / ESC 모두 작동 안 함
   - 페이지 새로고침으로만 탈출
   - **추정**: PaywallModal 같은 단일 컴포넌트 → 1번 수정으로 다 해결

2. 리포트 관리 카운트 vs 실제 내용 불일치
   - 전체(6)/공유됨(3)/임시저장(3) 카운트 표시
   - 화면은 "리포트 없음"
   - 다른 사용자 데이터 누수 또는 카운트 쿼리 오류

3. 문제 등록 — 학년 변경 시 영역 옵션 동기화 안 됨
   - 1학년 ↔ 2학년 바꿔도 영역 그대로
   
4. 자료실 폴더 시스템 무력화
   - 폴더 생성됨 → 자료가 폴더 안 들어가고 전체 표시
   - 폴더의 의미 없음

### 🟡 Important
5. 학부모 문자 캠퍼스 차단 (정책 어긋남)
   - decisions.md: 캠퍼스도 학생당 주 2회 가능
   - 현재: "스탠다드 이상" 차단
   - 캠퍼스 캘린더 활성화의 유일한 활용 명분이라 베타 전 필수

6. AI 유사문제 생성 자물쇠 없음 (decisions.md "보류")
7. 문제은행 → 시험에 추가 흐름 직관성 부족
8. 공유 PDF 파일명 토큰 노출 (1777213486308_c9f1xa.pdf?token=...)

### 🟢 Minor
9. 문제 등록 난이도 텍스트 깨짐 (JS 템플릿 문자열 노출)
10. "보기" 버튼이 다운로드 동작 (UX 일관성)
11. 학생 공유 모달도 동일 닫기 버그 가능성

### ✅ 정상 작동 확인
- 시험 관리: 새 시험 등록 모달 정상, 시험 등록 정상 작동, 토스트 알림 OK
- 자료실 폴더 생성 (구조만)
- 자료실 학생 공유 (모달은 OK, 닫기 미확인)

### 📌 점검 보류
- 리포트 공유 (테스트 안 됨)
- PDF/이미지 업로드 (이전 실험 충분)


# 점검 결과 종합 (2026-04-26)

## 🔴 Critical (베타 시작 전 무조건 수정)

### 1. 운영자 승인 시스템 깨짐
- 승인 대기 탭: "선생님 인증 시스템은 준비 중" 메시지 (비활성)
- 선생님 탭에서는 승인 가능하지만, 정상 흐름 아님
- 영향: 운영자가 신규 가입자 승인 못 함 → 베타 시작 자체 불가능

### 2. 자물쇠 모달 닫기 불가 (PaywallModal 추정)
- 회계 / 학생 관리 / (다른 자물쇠 메뉴들) 모두 동일 증상
- X / 닫기 / 업그레이드 / ESC 모두 작동 안 함
- 콘솔 에러 없음 (조용히 깨짐)
- 사용자가 갇힘 → 새로고침으로만 탈출
- **추정**: 단일 컴포넌트 → 1번 수정으로 다 해결 가능

### 3. 외래키 ON DELETE CASCADE 미적용 (시스템 차원)
- profiles/users 참조 외래키 37개
- 사용자 강제 삭제 불가 (Supabase UI/API 모두 실패)
- 30일 자동 영구삭제 트리거 작동 시 실패 위험
- 대응: delete_user_permanently RPC 함수 + 37개 테이블 순차 정리
- 또는 ON DELETE CASCADE 일괄 적용

### 4. 메일 시스템 - 본문 변수 누락
- "소속 기관: ―" 빈 상태로 발송 (DB엔 저장됨)
- EmailJS 템플릿 변수 매핑 버그
- 가입 메일 + 승인 메일 동일 문제

### 5. 메일 시스템 - 영문 placeholder 노출
- "A message by has been received. Kindly respond at your earliest convenience."
- EmailJS 기본 템플릿 텍스트가 본문에 노출
- 한국 사용자에게 신뢰도 떨어짐

### 6. 가입 시 티어 자동 배정 로직 부재
- 모든 신규 가입자 = 캠퍼스(무료) 자동 배정
- 선생님으로 가입해도 캠퍼스로 떨어짐 → 이상한 사용자 경험
- 가입 폼에 "업태 선택" 단계 없음 (decisions.md 4/26 정책 미반영)

### 7. 리포트 관리 카운트/내용 불일치
- 카운트: 전체(6) / 공유됨(3) / 임시저장(3)
- 화면: "리포트 없음"
- 다른 사용자 데이터 누수 또는 카운트 쿼리 오류

### 8. 문제 등록 - 학년/영역 동기화 안 됨
- 학년 변경 시 영역 옵션 안 바뀜
- 사용자가 잘못된 영역 선택 가능

### 9. 자료실 폴더 무력화
- 폴더 생성됨 → 자료 폴더 안 들어감
- 전체 자료가 폴더 무관하게 표시
- 폴더 시스템 의미 없음

## 🟡 Important (베타 전 처리하면 좋음)

### 10. 학부모 문자 캠퍼스 차단 (정책 어긋남)
- decisions.md: 캠퍼스도 학생당 주 2회 가능
- 현재: "스탠다드 이상" 차단
- 캠퍼스 캘린더 활성화의 유일한 활용 명분 → 베타 전 필수

### 11. 캠퍼스 가입 보너스 5,000pt 정책 미반영
- decisions.md: 5,000pt
- 현재: 1,000pt
- 4/26 결정 미반영

### 12. AI 유사문제 생성 자물쇠 없음
- decisions.md: "보류 (구현 후 결정)"
- 사용자 클릭 시 동작 미확인 → 자물쇠라도 걸어둘 것

### 13. 소속 기관 입력칸 두 줄 노출 (UX)
- 검색창 + 선택된 기관 칸이 동시에 보임
- 선택 후 검색창 숨김/비활성화 필요

### 14. 공유 PDF 파일명 토큰 노출
- 1777213486308_c9f1xa.pdf?token=... 형식
- 사용자 신뢰 저하

### 15. "보기" 버튼이 다운로드 동작 (UX 일관성)

## 🟢 Minor (베타 후)

### 16. 문제 등록 난이도 텍스트 깨짐
- JS 템플릿 문자열 노출 (${[1,2,3,4,5].map...)

### 17. 문제은행 → 시험 추가 흐름 직관성

### 18. 캠퍼스에서 대시보드 "2025년 1학기 · 3학년 2반" 자동 표시
- 신규 가입자에게 어떤 데이터인지?

## 🟢 정상 작동 확인

- 회원가입 페이지 (역할 5개, 입력칸, 약관 모달)
- 로그인
- 운영자 승인 후 대시보드 진입
- 캘린더 정상 표시 (오늘 날짜 강조 OK)
- 회계 자물쇠 (캠퍼스 차단 정책 반영)
- 시험 관리 (새 시험 등록, 모달, 토스트)
- 자료실 폴더 생성
- 메일 발송 (내용 문제 별개로, 발송 자체는 작동)

## ⏳ 베타 후 / 정식 런칭 전 (점검 안 함, decisions.md 결정 사항)

- 이메일 자동 인증 (Supabase Auth 활용)
- EmailJS → Supabase SMTP / SendGrid / Resend 전환
- 도메인 메일 (noreply@mathiq...)
- 가입 폼 업태 5개 선택 + 직원 형태 19개 분류
- 위장도급 진단 모달
- 직원 등록 가이드 시스템
- **회계 권한 위임 시스템** (owner/manager/teacher/staff)
  - manager 권한 세분화 (매출/지출/인건비/보고서)
  - "기타" 직접 입력 시 권한 체크박스 4개
- 캠퍼스 5,000pt 캠페인
- school_events (학교 공용 일정)
- 공휴일 자동 표시 (공공데이터포털)
- 알림톡 정식 도입 (100명+ 시점)

## 📊 점검 통계

- 점검 시간: 약 3시간 (2026-04-26 18:00~)
- 점검 메뉴: 사이드바 12개 중 9개
- 발견 이슈: Critical 9, Important 6, Minor 3 (= 18개)
- 정상 확인: 8가지
- 베타 시작 차단 이슈: 3개 (#1 승인, #2 자물쇠 모달, #3 외래키)

### 권한 매트릭스 시스템 (베타 후 핵심 기능)

원장이 직원별로 기능 권한을 체크박스로 켜고 끌 수 있는 매트릭스 UI.

#### 권한 카테고리 (대분류)
- 회계 (매출/지출/인건비/보고서 + 보기·입력·수정 분리)
- 학생 관리 (목록/등록/수정/삭제 + 학부모 정보)
- 강사 관리 (목록/등록/급여)
- 시험·문제 (시험 등록/문제 등록/채점)
- 학부모 소통 (문자 발송)
- 일정·캘린더 (추가/수정)

#### 권한 단위
- 각 카테고리 안에서 보기/입력/수정/삭제 분리 가능
- 원장이 직원별로 체크박스 매트릭스로 직접 설정

#### 의의
- 19개 직원 형태 분류와 결합 (직원 형태 = 권한 프리셋)
- "기타 직접 입력" 시 권한 체크박스 4개를 모든 직원에 확장
- 위장도급 리스크 분산 (원장이 명시적으로 권한 한정)
- 학원 ERP 차별화 핵심

#### 기술 의존
- DB: staff_permissions 테이블
- RLS: 권한 항목별 정책 30~40개
- 백엔드: 모든 페이지에 권한 체크 로직
- 프론트엔드: 매트릭스 UI

#### 베타 단계 처리
- 미구현 (봉쌤 1인 운영)
- 베타 후 5~10명 사용해보면서 실제 필요 권한 항목 검증
- 정식 런칭 전 구현


### 권한 매트릭스 시스템 (베타 후 핵심 기능)

#### 동작 방식
1. 원장이 직원 추가
2. 직원 형태 19개 중 1개 선택
   → 권장 권한이 매트릭스에 자동 체크됨
3. 원장이 직접 체크박스 추가/제거하여 미세 조정
4. 저장

#### 권한 카테고리 (6개)
- 회계 (매출/지출/인건비/보고서 + 보기·입력·수정 분리)
- 학생 관리 (목록/등록/수정/삭제 + 학부모 정보 접근)
- 강사 관리 (목록/등록/급여 보기)
- 시험·문제 (시험 등록/문제 등록/채점)
- 학부모 소통 (문자 발송)
- 일정·캘린더 (추가/수정)

#### 직원 형태별 권장 프리셋
- 19개 형태 × 권한 매트릭스 표
- 위험도 색상(🟢🟡🔴)에 맞춘 권한 강도
- 위장도급 리스크 직원(프리랜서)은 최소 권한
- 특수관계자(가족)는 빈 매트릭스에서 시작 → 원장이 명시

#### 기술 구현
- DB: staff_permissions 테이블 + permission_presets 테이블
- RLS: 권한 항목별 정책 30~40개
- 프론트엔드: 매트릭스 UI (체크박스 30~40개)
- 직원 형태 선택 시 프리셋 자동 적용 로직

#### 의의
- 19개 직원 형태와 권한 매트릭스 결합 → 학원 ERP 핵심 차별화
- 원장 부담 최소화 (프리셋 + 미세 조정)
- 위장도급 리스크 자연 분산 (원장이 명시적 권한 한정)
- decisions.md "회계 권한 위임 (Phase 2)" 의 진짜 모습

#### 베타 단계 처리
- 미구현 (봉쌤 1인 운영)
- 베타 후 5~10명 사용 검증
- 정식 런칭 전 구현 필수


### 권한 매트릭스 시스템 (베타 후 핵심 기능)

#### 동작 방식
1. 원장이 직원 추가
2. 직원 형태 19개 중 1개 선택
   → 권장 권한이 매트릭스에 자동 체크
3. 원장이 매트릭스 보면서 체크박스 추가/제거 (1단계: ON/OFF)
4. 저장

#### 도입 배경
- 학원 현장의 승진·역할 변경 반영
- "강사 → 강사 + 회계 보조 → 부원장" 같은 자연스러운 권한 변화 지원
- 한 번 정하면 끝이 아니라 계속 조정 가능

#### 권한 카테고리 (사이드바 메뉴와 1:1 매핑)
- 학습 관리: 대시보드 / 학생 관리 / 시험 관리
- AI·분석: AI 유사문제 / 성적 리포트 / 학부모 문자
- 회계 관리: 매출·지출
- 문제은행: 문제은행 / 문제 등록
- 자료: 수업 자료
- 공통: 캘린더 / 강사 관리

#### 권한 단위
- 1단계 (ON/OFF) — 메뉴 표시/차단 수준
- 베타에서 검증 후 필요 시 세분화 (보기/편집 분리 등)

#### 19개 직원 형태 권장 프리셋
[표 첨부]

핵심 결정:
- 부원장 = 강사 관리 ✅ / 실장 = 강사 관리 ❌ (선생-데스크 분리)
- 프리랜서·인적용역 = 정규직 동일 (회계 손 안 댐, 권한 풀 = 위장도급 안전)
- 가족·위탁·기타 = 빈 매트릭스 + 안내 문구

#### 안내 문구
- 일반: "권장 권한 자동 체크. 학원 운영에 맞춰 조정하세요."
- 위탁: "형태 다양 — 원장 직접 부여"
- 가족: "법적 리스크 — 보수적으로 선택"
- 기타: "권한·계약 직접 체크" (decisions.md 기존)
- 프리랜서: "권한 폭넓게 부여가 위장도급 리스크 회피"

#### 기술 구현
- DB: staff_permissions 테이블 + permission_presets 테이블
- RLS: 권한 항목별 정책 12~13개 (메뉴 단위라 단순)
- 프론트엔드: 매트릭스 UI (체크박스 12~13개 × 직원수)
- 직원 형태 선택 시 프리셋 자동 적용 로직

#### 베타 단계 처리
- 미구현 (봉쌤 1인 운영)
- 베타 후 5~10명 사용 검증
- 정식 런칭 전 구현 필수

### 발견: 탈퇴/복구 정책 (decisions.md 추가 필요)
- 자동 복구: 30일 안 50% 포인트 지급
- 이의신청 → 100% 복구
- 코드엔 구현됨, decisions.md엔 누락

---

## 🎯 오늘의 결과물 (2026-04-26 점검 마무리)

### decisions.md 업데이트
- 메일 시스템 처리 방향 (베타 전/후)
- 베타 혜택 검토 메모 (평생 pro → 스탠다드 변경 가능성)
- 출결 시스템 시장 조사 결과
- 티어 자동 배정 정책 (옵션 C 채택)
- 외래키 정리 진단 결과

### roadmap.md 업데이트 (05-roadmap.md 아님!)
- P0 네 개 완료 표시 (#15, #16, DB정리, 권한매트릭스)
- 🔥 점검 발견 이슈 섹션 추가 (Critical 6 / Important 6 / Minor 3)

### changelog.md 업데이트
- 2026-04-26 항목 추가 (버그 수정, 정책 확정, DB 정리, 문서화)

### 코드 수정 (commit ea1c730)
- closeModal() 함수: style.display 처리 추가
- tierLockModal: 배경 클릭 + ESC 키 닫기
- loadAdminPendingTab(): "준비 중" 제거, 실제 쿼리 활성화

### DB 정리
- 테스트 사용자 18명 삭제
- profiles 1행 / auth.users 1행 (admin만 남김)
- cleanup-test-users-v3.sql, cleanup-auth-users-orphans.sql 작성